spring:
  application:
    name: library-system
  jackson:
    default-property-inclusion: non_null

doma:
  naming: SNAKE_LOWER_CASE

jwt:
  secret: ${JWT_SECRET}
  expiration: 1800000

server:
  port: 8082

logging:
  level:
    root: WARN
